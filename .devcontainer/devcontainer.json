{
    "name": "Mission Planner Linux Build Env",
    "image": "mcr.microsoft.com/devcontainers/dotnet:8.0-bookworm",
    "features": {
        "ghcr.io/devcontainers/features/common-utils:1": {
            "configureZsh": true
        }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mono-complete msbuild nuget libvlc-dev libsqlite3-dev libcurl4-openssl-dev && nuget restore MissionPlanner.sln",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-dotnettools.csharp",
                "ms-vscode.mono-debug"
            ]
        }
    }
}
